# 달력 청약 일정 빠른 수정

- 달력 렌더가 `items` 기반 일정만 보던 흐름을 `items + events` 병합 방식으로 보강했습니다.
- 동일 날짜/유형/종목명 기준 중복은 제거했습니다.
- 달력 셀 정렬은 `청약 -> 상장 -> 보호예수` 우선순위로 바꿨습니다.
- 셀 안 표시 개수를 2건에서 3건으로 늘렸습니다.
- 정적 자산 캐시를 피하려고 `index.html`의 CSS/JS 버전 파라미터를 `20260412r8`로 올렸습니다.
